# rust-systems-tracker

A lightweight, async system diagnostic tracker built in Rust. It monitors CPU, memory, and process health, exposing a simple telemetry stream.

## Getting Started
```bash
cargo build --release
cargo run
```
